# 旅游计划：地图优先 V2 实施交付说明

> 更新时间：2026-07-21  
> 范围：地图优先行程规划 V2（定稿）Phase 0–4，以及此前已落地的地点/路线基础能力摘要。

---

## 1. 目标

把「三栏日程表单」升级为「全屏地图驱动的旅行规划」：

```
搜点 / 推荐 → 地点池 TripPlace → 今日草稿 RoutePlanDraft
→ 手动排序或 AI 规划 → 设时间 →「生成路线」→「保存行程」
→ ItineraryItem + RouteSegment
```

---

## 2. 已完成阶段总览

| 阶段 | 内容 | 状态 |
|------|------|------|
| Phase 0 | TripPlace + RoutePlanDraft 模型与迁移 | 完成 |
| Phase 1 | MapWorkspace 全屏页 + 地点池 + 手动草稿/时间 | 完成 |
| Phase 2 | 按钮触发多段路线预览 + confirm 落库 | 完成 |
| Phase 3 | AI 规划 API + 最近邻回退 | 完成 |
| Phase 4 | 移动端抽屉、序号 Marker、多段线分色、空状态 | 完成 |

验证（交付时）：`pytest` **58 passed**；`vitest` **9 passed**；`tsc` 通过。

---

## 3. 数据模型

### 保留

- `Trip`：旅行
- `Place`：地点坐标与 POI
- `ItineraryItem`：最终日程（activity / transport）
- `RouteSegment`：已确认交通段（仍绑定 after/before Item）

### 新增

**`trip_places`（地点池）**

- `status`：`candidate` / `selected` / `planned` / `removed`
- `order_index`、`preferred_duration`、`notes`
- `UNIQUE(trip_id, place_id)`
- 创建 Place 时自动入池；删除 Place 前先删池内引用

**`route_plan_drafts`（规划草稿）**

- `source`：`ai` / `manual`
- `status`：`draft` / `confirmed` / `cancelled`
- `stops_json`：停靠点列表（place_id、title、时间、order、停留分钟）
- 同一 `trip_id + date` 仅一个活跃 `draft`

迁移：`server/alembic/versions/0005_map_workspace.py`（含既有 Place 回填为 candidate）。

---

## 4. 后端 API

| 方法 | 路径 | 作用 |
|------|------|------|
| GET/POST | `/api/trips/{id}/trip-places` | 地点池列表 / 加入（幂等） |
| PATCH | `/api/trips/{id}/trip-places/{tp_id}` | 改状态/顺序/停留 |
| GET/PUT | `/api/trips/{id}/plan-drafts?date=` | 读/写当日草稿 |
| POST | `/api/trips/{id}/plan-drafts/{draft_id}/generate-routes` | 多段高德预览（不落 Segment） |
| POST | `/api/trips/{id}/plan-drafts/{draft_id}/confirm` | 生成 Item + Segment，草稿 confirmed |
| POST | `/api/trips/{id}/ai-plan` | AI 排序+时段；失败最近邻回退 |

相关服务：

- `server/app/services/trip_place_service.py`
- `server/app/services/plan_draft_service.py`
- `server/app/services/ai_plan.py`
- `server/app/services/route_resolve.py`（新增按 Place 对算路，无需先有 Item）
- `server/app/api/map_workspace.py`

AI 配置（`server/.env`）：

```env
AI_API_BASE_URL=...
AI_API_KEY=...
AI_MODEL=...
```

原则：AI 只排顺序与建议停留；真实公交/步行一律走高德。

---

## 5. 前端改造

- 路由仍为 `/trips/:tripId`，页面改为全屏 **MapWorkspace**（`web/src/pages/TripPlannerPage.tsx`）
- 顶栏：返回、日期切换、**AI 规划**、面板收起
- 地图：城市定位、序号 Marker、多段折线分色
- 底栏双栏：**今日路线**（排序/时间/生成路线/保存）+ **地点池**（推荐+已收藏）
- 去掉旧三栏与「关联地点」下拉

新增：

- `web/src/api/workspace.ts`
- `web/src/hooks/useWorkspace.ts`
- `web/src/types/workspace.ts`
- 地图相关：`marker-manager` 支持 label；`polyline-manager` 支持多段分色

---

## 6. 用户操作路径

1. 创建旅行并进入规划页（全屏地图）
2. 搜索或点城市推荐 → 地点进入池并打标
3. 「加入今日」或「AI 规划」得到当日 stops
4. 为各点设开始时间与停留分钟
5. 点「生成路线」（公交/地铁或步行）→ 地图画线、列表显示时长/换乘
6. 点「保存行程」→ 写入最终事项与交通段；刷新不丢

---

## 7. 此前相关基础（V2 之前已有）

- 日程 Trip/Item CRUD、半开区间冲突
- Place 搜索代理、2D 地图
- 路线 preview/persist、`preview_token`、RouteCache
- 城市选择器、城市地理编码中心、城市推荐（AI/本地回退）
- CORS：`127.0.0.1` / `localhost`；前端 API 指向 `http://127.0.0.1:8000`

总计划中尚未做完的后续项：附件上传、Markdown 导出（可再开阶段）。

---

## 8. 本地启动

```bash
# 后端
cd server && .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload

# 前端
cd web && npm run dev -- --host 127.0.0.1 --port 5173
```

浏览器打开：`http://127.0.0.1:5173`

---

## 9. 明确未做

- 跨城自动规划、实时公交、导航、订票、多人协作
- 地图上复杂拖拽编辑
- 排序时自动打高德（仅「生成路线」按钮触发）
- 旅行模板、完整导出流水线
